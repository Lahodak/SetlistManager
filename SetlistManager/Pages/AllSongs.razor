@page "/AllSongs"

<PageTitle>AllSongs</PageTitle>

<h3>All Songs</h3>

@if (_songCollection != null && _songCollection.Any())
{
    <ol>
        @foreach (var song in _songCollection)
        {
            <li class="py-1">
                <a href="@($"songdetail/{song.SongID}")" style="color: black; text-decoration: none; font-size: medium;">@song.ToString()</a>
            </li>
        }
    </ol>
}